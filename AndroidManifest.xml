<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.t3hh4xx0r.testplugin"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="15" />

    <application android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">

<!-- You need no activities unless you have some type of configuration or setup for your panel -->
	
	<receiver android:name=".MyPluginReceiver">
	  <intent-filter>
<!-- 	      You need to listen for the launchers request to register panels -->
	    <action android:name="com.t3hh4xx0r.haxlauncher.PANEL_REQUEST" />
	  </intent-filter>
	</receiver>	
    </application>

</manifest>
